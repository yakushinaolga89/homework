var a = 5;  
var b, c;

b = (a * 5); // обчислюється першим оскільки немає інших операторів з вищим пріоритетом
b = (c = (b / 2)); // вираз (b / 2) обчислюється другим, його результат буде присвоєно змінній c, значення змінної c буде присвоєно змінній b
