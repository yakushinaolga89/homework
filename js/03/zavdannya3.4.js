function divideWithoutRemainder(dividend, divisor) {
    return Math.floor(dividend / divisor);
}
const dividend = 40;
const divisor = 2;
const result = divideWithoutRemainder(dividend, divisor);
alert(result);