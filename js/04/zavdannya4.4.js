let text = "Порахуйте кількість слів у рядку";
let words = text.split(" ");
let count = words.length;

alert("Кількість слів у рядку: " + count);
alert("Довжина масиву: " + words.length);
alert("Довжина рядка: " + text.length);
